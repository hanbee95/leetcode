class Solution:
    def createTargetArray(self, nums: List[int], index: List[int]) -> List[int]:
        output = []
        for idx, num in enumerate(nums):
            output.insert(index[idx], num)
        return output  