class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
        #use set: remove duplicate elements!
        if len(nums) != len(set(nums)):
            return True
        else:
            return False
