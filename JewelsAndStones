class Solution:
    def numJewelsInStones(self, jewels: str, stones: str) -> int:
        output = 0
        for elem in stones:
            if elem in jewels:
                output = output + 1
        return output