class Solution:
    def runningSum(self, nums: List[int]) -> List[int]:
        output = []
        prev = 0
        for i in nums:
            prev = prev + i
            output.append(prev)
        return output